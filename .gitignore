# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Local / editor
.vscode/
.idea/
.DS_Store
*.log

# Monthly snapshots are regenerated by the pipeline; keep the repo lean by
# tracking only the latest snapshot. Remove these lines to archive every month.
# (data/latest.json IS committed as a live example.)
data/history/
